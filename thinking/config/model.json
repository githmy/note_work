{
  "max_features": 20000,
  "maxlen": 150,
  "embedding_size": 300,
  "batch_size": 16,
  "num_epochs": 100000,
  "max_learning_rate": 0.005,
  "min_learning_rate": 0.001,
  "decay_coefficient": 1.5,
  "dropout_keep_prob": 0.5,
  "evaluate_every": 100,
  "early_stop": 20,
  "l2_reg_lambda": 0.0,
  "num_filters": 128,
  "save_step": 200,
  "filter_sizes": [
    3,
    4,
    5
  ]
}